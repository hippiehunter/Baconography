<UserControl
    x:Class="SnooStream.View.Controls.PreviewTextControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnooStream.View.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="0,0,0,0" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid>
                <TextBlock  FontFamily="Segoe UI Symbol" FontSize="56" HorizontalAlignment="Right" VerticalAlignment="Center"
                                       Margin="0,12,0,0"
                                       Text="{Binding Converter={StaticResource linkGlyphConverter}}" />
            </Grid>
            
        </Border>
        <TextBlock Text="{Binding Synopsis}"/>
    </Grid>
</UserControl>
