<UserControl
    x:Class="SnooStream.View.Controls.MarkdownEditorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnooStream.View.Controls"
    xmlns:snoodom="using:SnooDom"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="root"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Margin="0,12,0,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
            <TextBlock Text="Posting as "/>
            <TextBlock Text="{Binding Path=PostingAs}"/>
        </StackPanel>

        <Button Grid.Column="1" Grid.Row="0"
                    HorizontalContentAlignment="Right"
                    BorderThickness="0" Padding="0" 
                    Margin="0,-12,-12,0"
                    FontFamily="Segoe UI Symbol">
            <Grid>
                <TextBlock Margin="0,0,0,0" FontSize="35" Text="&#xE10C;" />
            </Grid>
            <Button.Flyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Insert Link" Command="{Binding Path=InsertLink}"/>
                    <MenuFlyoutItem Text="Insert Bold" Command="{Binding Path=InsertBold}"/>
                    <MenuFlyoutItem Text="Insert Italic" Command="{Binding Path=InsertItalic}"/>
                    <MenuFlyoutItem Text="Insert Quote" Command="{Binding Path=InsertQuote}"/>
                    <MenuFlyoutItem Text="Insert Bullet" Command="{Binding Path=InsertBullet}"/>
                    <MenuFlyoutItem Text="Insert List" Command="{Binding Path=InsertList}"/>
                    <MenuFlyoutItem Text="&#x0CA0;_&#x0CA0;" Command="{Binding Path=InsertDisapproval}"/>
                </MenuFlyout>
            </Button.Flyout>
        </Button>

        <TextBox x:Name="textBox" KeyUp="TextBox_KeyUp" AcceptsReturn="True" IsSpellCheckEnabled="True" TextChanged="TextBox_TextChanged" InputScope="Chat" 
                 Text="{Binding Path=Text, Mode=TwoWay}" HorizontalAlignment="Stretch" TextWrapping="Wrap" Grid.Row="1" Grid.ColumnSpan="2" />

        <snoodom:MarkdownControl Markdown="{Binding Path=Markdown}" Grid.Row="2" Grid.ColumnSpan="2"
                StyleProvider="{StaticResource markdownHelpers}"
                CommandFactory="{StaticResource markdownHelpers}"
                HorizontalAlignment="Stretch" />
    </Grid>
</UserControl>
