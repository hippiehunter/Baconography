<UserControl
    x:Class="SnooStream.View.Controls.OverlayControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnooStream.View.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    MaxHeight="200" MinHeight="75"
    >

    <Grid x:Name="overlay" Background="{ThemeResource ContentBackgroundBrush}"
          HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,24,0,6">
            <TextBlock HorizontalAlignment="Right" FontSize="22" Margin="12,0,12,0"
                Text="{Binding Title}" TextWrapping="Wrap" 
                x:Name="caption"/>
        </ScrollViewer>
    </Grid>
</UserControl>
