<UserControl
    x:Class="SnooStream.View.Controls.CardView.CardMarkdownControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnooStream.View.Controls.CardView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:snoodom="using:SnooDom"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <snoodom:MarkdownControl x:Name="markdownControl"
                Markdown="{Binding}"
                StyleProvider="{StaticResource markdownHelpers}"
                CommandFactory="{StaticResource markdownHelpers}"
                HorizontalAlignment="Stretch"
                Margin="0,12,0,12"/>
</UserControl>
