<UserControl
    x:Class="SnooStream.View.Controls.Content.ImageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnooStream.View.Controls.Content"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch" x:Name="scrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsHorizontalRailEnabled="True" ZoomMode="Enabled">

            <Image x:Name="image" HorizontalAlignment="Center" VerticalAlignment="Center" 
               DataContext="{Binding ImageLoader.ImageData, Converter={StaticResource gifConverter}}" Stretch="UniformToFill" Source="{Binding ImageSource}" SizeChanged="Image_SizeChanged"/>

        </ScrollViewer>
    </Grid>
</UserControl>
