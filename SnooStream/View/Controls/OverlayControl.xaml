<UserControl
    x:Class="SnooStream.View.Controls.OverlayControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnooStream.View.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    MaxHeight="200" MinHeight="75"
    >

    <Grid x:Name="overlay" Background="{ThemeResource ContentBackgroundBrush}"
          HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <TextBlock Style="{StaticResource TitleTextBlock}" Margin="20,24,20,6" Text="{Binding Title}" x:Name="caption"/>
    </Grid>
</UserControl>
